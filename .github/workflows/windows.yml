name: rdp-windows

on:
  workflow_dispatch:
  push:

jobs:
  rdp:
    runs-on: windows-latest
    env:
      GH_TOKEN: ${{ secrets.GH_PAT }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        
      - name: Check path
        run: dir

      - name: Checkout private tools
        uses: actions/checkout@v1
        with:
          repository: fkunn1326/ghvnc-secret
          token: ${{ secrets.GH_PAT }}
          path: C:\Users\runneradmin\AppData\Local\Google\Chrome\User Data
      
      - name: Backup files
        run: .\scripts\Backup.ps1　${{ secrets.GH_PAT }}
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
