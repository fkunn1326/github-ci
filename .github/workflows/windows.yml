name: rdp-windows

on:
  workflow_dispatch:
  push:

jobs:
  rdp:
    runs-on: windows-latest
    env:
      GH_TOKEN: ${{ secrets.GH_PAT }}
    steps:
      - name: Clone repo
        run: gh repo clone fkunn1326/github-ci .
          
      - name: Clone backup
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: git clone https://fkunn1326:${GH_PAT}@github.com/fkunn1326/ghvnc-secret
      
      - name: Backup files
        run: .\scripts\Backup.ps1　${{ secrets.GH_PAT }}
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
